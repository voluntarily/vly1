// Import Actions
import { } from './OrganisationActions';

// Initial State
const initialState = {};

const OrganisationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default OrganisationReducer;
