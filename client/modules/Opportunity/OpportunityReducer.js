// Import Actions
import { } from './OpportunityActions';

// Initial State
const initialState = {};

const OpportunityReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default OpportunityReducer;
